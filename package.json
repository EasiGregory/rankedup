{
  "name": "rankedup",
  "private": true,
  "devDependencies": {
    "bower": "^1.8.0",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 src/main/app/"
  }
}
